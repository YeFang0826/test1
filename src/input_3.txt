@pre: n == |a|
@post: sorted(a, 0, |a|-1)
void selectionSort(int[] a){
 	int i;
	int j;
	int iMin;
	int temp;

	for @ L: sorted(a, 0, j) and (j>0 => (forall(k), j<=k and k<=|a|-1 => a[j-1] <=a[k] ))
	( j=0; j<n-1; j=j+1){
		iMin = j;
		for @L: forall(j), j<=k and k<= i-1 => a[iMin]<=a[k]
		( i=j+1;  i<n; i=i+1){
			if(a[i] < a[iMin]){
				iMin = i;
			}
		}
		if(iMin != j){
			temp = a[j];
			a[j] = a[iMin];
			a[iMin] = temp;
		}
	}
}